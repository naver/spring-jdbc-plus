compileJava.dependsOn(processResources)

dependencies {
    compile("org.springframework.data:spring-data-jdbc:2.0.1.RELEASE")
    compile("org.springframework.data:spring-data-relational:2.0.1.RELEASE")
    compile("org.springframework.data:spring-data-commons:2.3.1.RELEASE")

    testImplementation("org.junit.jupiter:junit-jupiter")
    testImplementation("org.junit.jupiter:junit-jupiter-api")
    testImplementation("org.junit.jupiter:junit-jupiter-params")
    testImplementation("org.junit.jupiter:junit-jupiter-engine")
    testImplementation("org.assertj:assertj-core")
    testImplementation("org.mockito:mockito-junit-jupiter")

    testRuntime("org.junit.platform:junit-platform-launcher")
    testRuntime("org.junit.platform:junit-platform-commons")
}
